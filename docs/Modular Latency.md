Latency = delay before something starts.- High latency → slow, laggy
- Low latency → fast, responsive

Modular - Load only what you need. .Net Core onwards (1,2,3...5...8) are modular. Example, loading nuget packages.
Kind of buffet. While the old .Net framework (till 4.8) loads all assemblies and becomes heavy
